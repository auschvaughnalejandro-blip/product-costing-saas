/**
 * @costing/shared — types shared between the API and the web frontend.
 *
 * Keeping these in one place means the engine, the API, and the UI all agree
 * on exactly what a "product node" or a "cost result" looks like.
 */

export * from './common';
